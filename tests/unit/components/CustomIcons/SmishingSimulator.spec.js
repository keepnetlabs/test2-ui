import SmishingSimulator from '@/components/CustomIcons/SmishingSimulator.vue'

describe('SmishingSimulator.vue', () => {
  it('has expected component name', () => {
    expect(SmishingSimulator.name).toBe('SmishingSimulatorIcon')
  })
})
