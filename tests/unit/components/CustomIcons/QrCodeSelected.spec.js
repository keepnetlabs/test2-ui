import QrCodeSelected from '@/components/CustomIcons/QrCodeSelected.vue'

describe('QrCodeSelected.vue', () => {
  it('has expected component name', () => {
    expect(QrCodeSelected.name).toBe('QrCodeSelected')
  })
})
