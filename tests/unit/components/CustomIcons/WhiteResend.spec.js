import WhiteResend from '@/components/CustomIcons/WhiteResend.vue'

describe('WhiteResend.vue', () => {
  it('has expected component name', () => {
    expect(WhiteResend.name).toBe('WhiteResend')
  })
})
