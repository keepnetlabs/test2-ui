import CreateNewInstance from '@/components/CustomIcons/CreateNewInstance.vue'

describe('CreateNewInstance.vue', () => {
  it('has expected component name', () => {
    expect(CreateNewInstance.name).toBe('CreateNewInstance')
  })
})
