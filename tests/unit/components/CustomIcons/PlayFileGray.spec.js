import PlayFileGray from '@/components/CustomIcons/PlayFileGray.vue'

describe('PlayFileGray.vue', () => {
  it('has expected component name', () => {
    expect(PlayFileGray.name).toBe('PlayFileGray')
  })
})
