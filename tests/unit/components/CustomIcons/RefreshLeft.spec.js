import RefreshLeft from '@/components/CustomIcons/RefreshLeft.vue'

describe('RefreshLeft.vue', () => {
  it('has expected component name', () => {
    expect(RefreshLeft.name).toBe('RefreshLeft')
  })
})
