import LockOpenTime from '@/components/CustomIcons/LockOpenTime.vue'

describe('LockOpenTime.vue', () => {
  it('has expected component name', () => {
    expect(LockOpenTime.name).toBe('LockOpenTime')
  })
})
