import DataTableAttachment from '@/components/DataTableComponents/DataTableAttachment.vue'

describe('DataTableAttachment.vue', () => {
  it('is a functional component with expected name', () => {
    expect(DataTableAttachment.name).toBe('DataTableAttachment')
    expect(DataTableAttachment.functional).toBe(true)
  })
})
